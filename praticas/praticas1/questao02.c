#include <stdio.h> 

int main () {

  float base = 2.0;
  float altura = 6.0; 
  float area = 2.0;

  printf("Entre com a base do triangulo: ");
  scanf("&f", &base);

  printf("Entre com a altura do triangulo");
  scanf("&f", &altura);

  printf("O valor da base é %f\n", base);")
  scanf(base);
  
   float area = base * altura / 2;
  printf ("A base do triangulo é: %f\n", area);
  
  
  return 0;

  // fazer um programa em c que calcule a area de um triangulo (a=base x altura/2) obs: considerar a base e a altura inteiros maiores que 0//
  
}